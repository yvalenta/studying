<%= form_with(model: link, local: true) do |form| %>
  <% if link.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(link.errors.count, "error") %> prohibited this link from being saved:</h2>

      <ul>
      <% link.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :link %>
    <%= form.text_field :link, id: :link_link %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description, id: :link_description %>
  </div>

  <div class="field">
    <%= form.label :category_id %>
    <%#= form.text_field :category_id, id: :link_category_id %>
    <%= form.select :category_id, options_from_collection_for_select(@categories, 'id', 'name_category') %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
